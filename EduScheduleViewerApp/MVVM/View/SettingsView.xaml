<UserControl x:Class="EduScheduleViewerApp.MVVM.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EduScheduleViewerApp.MVVM.View"
             xmlns:viewModel="clr-namespace:EduScheduleViewerApp.MVVM.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="640" d:DesignWidth="1100"
             Background="#fff">
    <UserControl.DataContext>
        <viewModel:SettingsViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="284"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Margin="16">
            <RadioButton Content="Розклад" Style="{StaticResource SettingsTabButtonStyle}"
                         IsChecked="True"/>
            <RadioButton Content="Звук" Style="{StaticResource SettingsTabButtonStyle}"/>
        </StackPanel>

        <UserControl Grid.Column="1" Content="{Binding CurrentSettingsTab}"/>
    </Grid>
</UserControl>
