<Window x:Class="EduScheduleViewerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EduScheduleViewerApp"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        FontSize="20" FontFamily="Roboto" Foreground="#2F2F2F"
        Title="Edu Schedule Viewer" Width="1100" Height="640">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Column="0" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Edu Schedule Viewer" FontSize="25" FontWeight="Bold"
                       Margin="0 0 16 0"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Style="{StaticResource IconButton}" Tag="FitToScreen" Margin="0 0 8 0"/>
                <Button Style="{StaticResource IconButton}" Tag="CogOutline"/>
            </StackPanel>

        </Grid>

        <ContentControl Grid.Row="1" Content=""/>
    </Grid>
</Window>
